name: Benchmark Gecko (Linux/macOS)

on:
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  benchmark:
    name: Benchmark Gecko (${{ matrix.os }})
    timeout-minutes: 120
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Install ripgrep
        run: cargo install ripgrep --locked

      - name: Build release
        run: cargo build --release

      - name: Free disk space (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache
          df -h

      - name: Run benchmark
        run: |
          chmod +x scripts/benchmark.sh
          ./scripts/benchmark.sh --queries scripts/benchmark-queries-gecko.json --skip-build --bench-dir /tmp/tgrep-bench --results benchmark-results.md

      - name: Print results
        run: cat benchmark-results.md

      - name: Upload results
        uses: actions/upload-artifact@v4
        with:
          name: benchmark-gecko-${{ matrix.os }}
          path: benchmark-results.md
